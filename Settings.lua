--- Localize Vars
-- Addon
local addonName, AT = ...;

-- All settings here should be moved into the GUI someday
AT.GUISettings = {
  Nameplates = {
    TTD = {
      Enabled = true,
      -- Default Nameplate
        --XOffset = 5,
        --YOffset = -11
      -- Kui Nameplate
        XOffset = 16,
        YOffset = -16
    }
  }
};
